Расставьте строки кода таким образом, чтобы получился метод принимающий два параметра и возвращающий их среднее арифметическое.

Расположите элементы списка в правильном порядке
Верно решили 442 учащихся
Из всех попыток 81% верных
 Здорово, всё верно.

def avg(x, y)
  res = (x + y).to_f/2
  return res
end