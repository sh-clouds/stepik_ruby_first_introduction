Класс может иметь несколько переменных экземпляра. Например:

class Car
  def initialize(type, wheels, color)
    @type = type
    @wheels = wheels
    @color = color
  end
end

volvo = Car.new("Грузовик", 6, "Белый")