Создайте класс Cat. Используя метод инициализации реализуйте переменную экземпляра age, которая должна принимать значение переменной, переданной в метод new при создании нового объекта.

Создавайте только класс согласно условию выше. Новый объект создавать не надо!

Напишите программу. Тестируется через stdin → stdout
Верно решили 1 290 учащихся
Из всех попыток 66% верных


# put your ruby code here
# нужно реализовать только класс. Новый объект создавать не нужно
class Cat
    def initialize(ageCat)
        @age = ageCat
    end
end