Напишите класс Star, имеющий переменную экземпляра distance. Создайте методы get_distance и set_distance= соответственно для доступа к переменной и для ее изменения.

Напишите программу. Тестируется через stdin → stdout
Верно решили 1 197 учащихся
Из всех попыток 23% верных


# put your ruby code here
class Star
    def get_distance
        @distance
    end
    
    def set_distance=(dist) 
        @distance = dist
    end    
end