Осталось только создать два объекта Player и вызвать метод fight. Чтобы сделать игру интересной, мы можем использовать случайные значения для здоровья и силы наших игроков, используя метод rand, который возвращает случайное значение в диапазоне от 0 до своего аргумента.

p1 = Player.new("Игрок 1", 1 + rand(100), 1 + rand(20))
p2 = Player.new("Игрок 2", 1 + rand(100), 1 + rand(20))

# показываем информацию об игроках
show_info(p1, p2)

puts "ДА НАЧНЕТСЯ БОЙ!"
fight(p1, p2)

                  
Мы использовали 100 как максимальное значение для здоровья и 20 как максимальное значение для силы. Мы добавили 1 к методу rand, чтобы избежать значения 0.

Теперь каждый раз при запуске программы будут создаваться два игрока со случайным здоровьем и силой, которые будут сражаться!

Окончательный код:

class Player
  attr_accessor :name, :health, :power
  def initialize(n, h, pow)
    @name = n
    @health = h
    @power = pow
  end
  def isAlive
    @health > 0
  end
  def hit(opponent)
    opponent.health -= self.power
  end
  def to_s
    "#{name}: Здоровье: #{health}, Сила: #{power}"
  end
end

def fight(p1, p2)
  while p1.isAlive && p2.isAlive
    p1.hit(p2)
    p2.hit(p1)      
    show_info(p1, p2)
  end
    
  if p1.isAlive 
    puts "#{p1.name} ПОБЕДИЛ!" 
  elsif p2.isAlive
    puts "#{p2.name} ПОБЕДИЛ!" 
  else
    puts "НИЧЬЯ!"
  end
end

def show_info(*p)
  p.each { |x| puts x}
end

# создаем игроков
puts "ИНФОРМАЦИЯ ОБ ИГРОКАХ"
p1 = Player.new("Игрок 1", 1 + rand(100), 1 + rand(20))
p2 = Player.new("Игрок 2", 1 + rand(100), 1 + rand(20))

# показываем информацию об игроках
show_info(p1, p2)

puts "ДА НАЧНЕТСЯ БОЙ!"
fight(p1, p2)

                  
Это упрощенная версия игры. Вы можете легко создавать различные подклассы игроков, добавлять больше свойств, определять оружие, получать пользовательский ввод для принятия различных решений и так далее, насколько хватит вашей фантазии и умений.