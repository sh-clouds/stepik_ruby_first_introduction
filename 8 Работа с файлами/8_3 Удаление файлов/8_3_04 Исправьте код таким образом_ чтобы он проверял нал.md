Исправьте код таким образом, чтобы он проверял наличие файла sample.txt и выводил его содержимое, если файл существует, а если этого файла не существует - выводил бы надпись Файл не существует

Напишите программу. Тестируется через stdin → stdout
Верно решили 897 учащихся
Из всех попыток 66% верных


# put your ruby code here
if File.file?("sample.txt")
  file = File.open("sample.txt")
  puts file.read
  file.close
else
  puts "Файл не существует"
end