Открытие файлов

Если нужно открыть уже существующий файл, используется метод open класса File:

file = File.open("sample.txt", "w+") 

                  
Как и в методе new, второй аргумент метода open задает режим работы с файлом.