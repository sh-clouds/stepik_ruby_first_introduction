Следующий оператор сравнения это оператор не равно !=, который возвращает true, если сравниваемые операнды не равны, и false, если они равны.

Например:

x = 5
y = 2
puts x != y     # выведет: true
puts x != 5     # выведет: false
puts x != y + 3 # выведет: false