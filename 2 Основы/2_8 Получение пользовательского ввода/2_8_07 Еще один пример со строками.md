Еще один пример со строками.

На стандартный вход подается имя. Нам надо его считать и вывести строку с приветствием:

name = gets                       # считываем имя
puts "Добро пожаловать, " + name  # выводим строку

                  
Вот так это будет выглядеть в редакторе Степика:

